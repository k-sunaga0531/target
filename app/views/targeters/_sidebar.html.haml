.sidebar-headder
  = link_to "所属グループ一覧", new_group_path
.sidebar-main
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to group_targeters_path(group) do
          .group__name
            = group.group_name
          .group__target
            -# = group.show_currentuser_targeter_name
            -# = targeter.targeter_name