.target-headder
  .current-group
    .current-group__group-name
      = @group.group_name
    .current-group__group-member
      - @group.users.each do |user|
        = user.user_name
  .form
  = form_for [@group, @targeter] do |f|
    = f.text_field :targeter_name, class: 'form__targeter', placeholder: 'type a targeter'
    = f.submit 'Send', class: 'form__submit'
  -# .edit-btn
  -#   編集

  保存機能は実装。あとはビューの表示
.target-main
  .targets
    .first-target
      .targeter-name
        - if targeter.targeter_name.present?
          %p.targeter-name__name
            = targeter.targeter_name(@user,@group)
        -# 腕立て1日30回
        -# = link_to "腕立て1日30回", edit_group_targeter_path(@group)
      .target-counts
      .target-user
        = targeter.user_id_change
    .second-target
      .targeter-name
        30分ジョギング
      .target-counts
      .target-user
        しほ
    .third-target
      .targeter-name
        回し車で夜中走る
      .target-counts
      .target-user
        はむお
-# .form
-#   = form_for [@group, @targeter] do |f|
-#     = f.text_field :targeter_name, class: 'form__targeter', placeholder: 'type a targeter'
-#     .form__mask
-#       -# = f.label :image, class: 'form__mask__image' do
-#         -# = icon('fas', 'image', class: 'icon')
-#         -# = f.file_field :image, class: 'hidden'
-#     = f.submit 'Send', class: 'form__submit'