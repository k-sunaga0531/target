=form_for @group do |f|
  = f.text_field :group_name, class: "target-group-form__input", placeholder: "グループ名を入力してください"
  = f.collection_check_boxes :user_ids, User.all, :id, :user_name
  = f.submit "Save", class: "target-group-form__action-btn"