
= form_for @group do |f|
  .group-form__errors
    %h2 10件のエラーが発生しました
    %ul
      %li nameを入力してください
  .group-form__field
    .group-form__field--left
      = f.label :group_name, class: 'group-form__label'
    .group-form__field--right
      = f.text_field :group_name, class: 'group_name group-form__input', placeholder: 'グループ名を入力してください'
  .group-form__field.clearfix
    / この部分はインクリメンタルサーチ（ユーザー追加の非同期化のときに使用します
  .group-form__field.clearfix
    .group-form__field--left
      %label.group-form__label{:for => "group_チャットメンバー"} チャットメンバー
    .group-form__field--right
      / グループ作成機能の追加時はここにcollection_check_boxesの記述を入れてください
      = f.collection_check_boxes :user_ids, User.all, :id, :user_name
      / この部分はインクリメンタルサーチ（ユーザー追加の非同期化のときに使用します
  .group-form__field.clearfix
    .group-form__field--left
    .group-form__field--right
      = f.submit class: 'group-form__action-btn'

